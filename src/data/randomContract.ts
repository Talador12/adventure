// Random contract generator — legal/shady contracts NPCs offer the party.
export interface Contract { title: string; parties: string; terms: string; payment: string; penalty: string; loophole: string; duration: string; }
const TITLES = ['Mercenary Services Agreement', 'Escort Contract', 'Non-Disclosure Pact', 'Bounty License', 'Property Recovery Agreement', 'Mutual Defense Treaty', 'Salvage Rights Claim', 'Blood Oath of Service'];
const TERMS = ['Retrieve the specified item within the timeframe.', 'Protect the client from all threats during travel.', 'Never speak of what you see or hear.', 'Eliminate the target by any means necessary.', 'Investigate the disappearances and report findings.', 'Guard the location for the specified duration.', 'Deliver the sealed package unopened.', 'Clear the dungeon and surrender all magical items found.'];
const PAYMENTS = ['500gp upon completion', '200gp upfront + 300gp on delivery', 'A favor from a powerful noble', 'Deed to a small property', 'A rare magic item from the client\'s collection', 'Free passage through restricted territory for 1 year', '1,000gp split among the party'];
const PENALTIES = ['Forfeit of all payment + reputation damage', 'A bounty placed on the party\'s heads', 'A curse activates (written into the contract with arcane ink)', 'The client\'s guild blacklists the party', 'Imprisonment for breach of contract', 'Nothing — the contract is unenforceable (but the client doesn\'t know that)'];
const LOOPHOLES = ['The contract doesn\'t specify *which* item — any item matching the description counts.', 'The timeframe is listed in "business days" — weekends don\'t count.', '"By any means necessary" technically includes negotiation.', 'The penalty clause references a law that was repealed last year.', 'The contract is only valid if both parties sign — the client forgot.', 'No loophole. This contract is iron-clad.'];
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)]; }
export function generateContract(): Contract { return { title: pick(TITLES), parties: 'The Party & [Client Name]', terms: pick(TERMS), payment: pick(PAYMENTS), penalty: pick(PENALTIES), loophole: pick(LOOPHOLES), duration: pick(['7 days', '30 days', 'Until completion', '1 year', 'Permanent']) }; }
export function formatContract(c: Contract): string { return `📜 **${c.title}**\n👥 Parties: ${c.parties}\n📋 Terms: ${c.terms}\n💰 Payment: ${c.payment}\n⚠️ Penalty for breach: ${c.penalty}\n⏰ Duration: ${c.duration}\n🤫 Loophole: ${c.loophole}`; }
